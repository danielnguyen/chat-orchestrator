ORCH_API_KEY=change_me

# Docker network default (when orchestrator runs in compose with memory-store service name).
# For local non-docker runs, use: http://127.0.0.1:4321
MEMORY_STORE_BASE_URL=http://basic-memory-store:8000
MEMORY_STORE_API_KEY=change_me

# Docker network default (when LiteLLM runs as a compose service name).
# For local non-docker runs, this is often still: http://127.0.0.1:4000
LITELLM_BASE_URL=http://litellm:4000
LITELLM_API_KEY=

ROUTER_RULES_PATH=/app/api/router/rules.yaml
MODEL_REGISTRY_PATH=/app/api/router/model_registry.yaml

ALLOW_MANUAL_OVERRIDE=true
DEFAULT_PROFILE_NAME=dev
OFFLINE_PROVIDER=litellm-local
OLLAMA_BASE_URL=
REQUEST_TIMEOUT_MS=30000
